/** @type {import('next').NextConfig} */
const nextConfig = {
    // reactStrictMode: false, //adicionado para parar de dar erro de  <React.StrictMode> no console do Opera
};

export default nextConfig;
